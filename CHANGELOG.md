# Changelog

## v0.1.13 - 2026-08-16

### 修复（Fixes）
- **恢复快照扫描兼容性**：修复 v0.1.12 在部分主机（如 TauriTavern）的 `/api/characters/chats` 不返回 `chat_metadata` 时，已有 `[FA]` 快照在面板中消失、新的 roll / delete / edit / rescue 快照刷新后不可见的问题。现在 catalog 缺省元数据时会自动回退到逐个 `/api/chats/get` 读取分支头信息，兼容不支持 catalog 元数据的主机，同时保留支持该字段主机的单请求快速路径。
- **回归覆盖**：新增 TauriTavern 风格 catalog（无 `chat_metadata`）的 mock host 测试，并在本地 SillyTavern 上通过模拟无元数据 catalog 验证已有快照可重新发现、新 roll 快照可正常显示。

## v0.1.12 - 2026-08-14

### 可靠性与性能（Reliability & Performance）
- **安全、可重试的迁移**：旧分支编号迁移支持从新旧编号混合状态继续；快照迁移失败时延后 root 改写，catalog 不可用也会明确报告失败。
- **串行化存储操作**：扫描、快照、迁移、预览读取与删除通过失败隔离的队列执行，避免聊天生命周期事件和文件写入交错。
- **大分支树优化**：缓存派生分支索引，优先使用聊天目录摘要；只有可见行需要正文回退时才惰性读取完整聊天，重渲染期间的请求结果也不会丢失。
- **可见保留策略**：默认永久保留全部快照，也可启用数量提醒；插件不会在后台自动删除文件。

### 安全与行为修正（Safety & Behavior）
- **稳定分支编号**：Prune 仅删除叶子快照，不再重排其余分支 id，避免部分重命名失败破坏树结构。
- **持久化与渲染加固**：失败保存保持可重试，源文件删除受到显式策略保护，导入元数据与面板动态文本经过严格验证和安全 DOM 渲染。
- **生命周期回归覆盖**：新增带状态的 SillyTavern host fixture，覆盖保存、删除、重命名、catalog、迁移重试、惰性预览与聊天事件顺序。

## v0.1.11 - 2026-08-04

### 新增（New）
- **当前节点标记**：面板用金色高亮和 `current` 徽标标出当前所在的节点，一眼就能看出你是在主主干（`br_000`）上，还是误触到了某个分支快照。
- **一键返回主主干**：当当前聊天是分支快照时，面板标题栏会出现 `⤺ Main` 按钮，点击立即切回该聊天树的主 root，不再担心"误触分支后找不到主线"。

## v0.1.10 - 2026-08-04

### 修复（Fixes）
- **快照归属自愈**：修复旧版本迁移遗留的 `main_chat` 失效问题。此前若某个快照的 `main_chat` 指向一个已被更名的旧文件，打开该快照后面板会退化成"没有 root 的孤儿分支树"；现在会自动沿父级链条解析出真正的聊天根（`br_000`），并把修正写回文件。
- **聊天归属消歧**：不同聊天共用 `br_000` / `br_000-1` 等编号，解析归属时不再按编号盲取第一个匹配，而是结合合法 `main_chat`、文件名前缀血缘和唯一候选逐级消歧，避免跨聊天串树。
- **活根兜底**：只要当前聊天是真实聊天文件（非 `[FA]` 快照），面板就保证显示 `br_000` 根节点，任何情况下都不会只剩分支树。

## v0.1.9 - 2026-08-04

### 修复与优化（Fixes & Improvements）
- **根节点折叠生效**：现在任意父节点（包括 `br_000` 根节点）都能折叠/展开整个子树，折叠后分支树只显示该节点本身。
- **窗口自适应高度**：面板高度随折叠/展开状态自动收缩或增长，但始终严格不超过最大边界（桌面 70vh、移动端约半屏），内容超出时仍在面板内部滚动。
- **位置安全**：面板尺寸变化或从隐藏状态重新打开时，若窗口被拖拽过，会自动重新钳制在屏幕安全区内，不会被内容撑出屏幕。
- **折叠状态按聊天隔离**：每个聊天的折叠/展开与搜索状态互不影响，切换聊天时面板自动回到该聊天的独立视图。

## v0.1.8 - 2026-08-04

### 修复（Fixes）
- **面板固定尺寸**：面板高度固定（桌面 70vh、移动端 50dvh），分支树再多也不会把窗口越撑越大；超出部分在面板内部滚动。
- **入口图标单击防护**：修复点击插件图标打开面板时，若某个分支按钮恰好落在同一位置，会被同一次点击误触发的 bug（现在只会有打开/关闭一个动作）。

## v0.1.7 - 2026-08-04

### 新增（New）
- **新建角色消息楼层**：当回复卡在 thinking、正文出不来导致楼层不可编辑时，可在面板里直接新建一个角色楼层并粘贴内容（“+”按钮 → Add floor，或 Ctrl+Enter）。
- 追加前自动生成 `rescue` 快照，之后可从面板回退到追加前状态。
- 追加后立即落盘，避免防抖保存被取消导致楼层丢失；失败时输入内容保留，不会被锁死。

## v0.1.6 - 2026-08-04

### 修复与优化（Fixes & Improvements）
- 所有聊天的根节点统一显示为 `br_000`（此前未管理的聊天会显示原始聊天文件名）。
- 未管理聊天（尚未写入元数据）也显示正文预览。
- 行操作 Switch / Prune 改为紧凑的图标按钮（回退 / 垃圾桶），悬停有提示。
- 安全护栏：根节点不再显示 Prune，防止误删当前聊天。
- 验证了角色卡与同角色不同聊天的隔离（含同名聊天的跨角色隔离）。

## v0.1.5 - 2026-08-04

- **按聊天隔离撤销树**：每个聊天独立一棵 `br_000` 树，切换聊天 / 角色卡时面板自动切换，不再互相污染。

## v0.1.4 - 2026-08-04

- 面板改为居中浮动窗口，支持拖拽移动（桌面与移动端）。

## v0.1.3 - 2026-08-04

- 修复 TauriTavern 移动端面板被强制全屏的问题（改用 `free-window` 表面）。

## v0.1.2 - 2026-08-04

- 移动端面板占位限制为约半屏（`50dvh`）。

## v0.1.1 - 2026-08-04

- 修复移动端面板跑出屏幕的问题（`body` 被 ST 设为 fixed 容器导致定位失效）。

## v0.1.0 - 2026-08-02

- 首个发布版本：楼层级快照与回退面板。
- 支持 GitHub 链接安装，兼容 TauriTavern 扩展管理器（绝对路径导入）。
